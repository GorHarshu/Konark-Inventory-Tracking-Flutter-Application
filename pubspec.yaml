name: konark_inventory_tracking_flutter_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  google_fonts: ^6.2.1
  hive: ^2.2.3
  path_provider: ^2.1.5
  http: ^1.3.0
  connectivity_plus: ^6.1.2
  in_app_update: ^4.2.3
  qr_code_scanner_plus: ^2.0.10+1
  image_picker: ^1.1.2
  dart_amqp: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
